import { GenericResponseInterceptor } from './generic-response.interceptor';

describe('GenericResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new GenericResponseInterceptor()).toBeDefined();
  });
});
